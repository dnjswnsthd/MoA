<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moa.model.mapper.ProjectMapper">
	<!-- project create -->
	<!-- 펀딩 오픈 -->
	<insert id="create" parameterType="Map">
		insert into project(project_name, start_date, end_date, category, participants, funding_cost, description, mentor_chk, deadline)
		values
		(#{project_name}, #{start_date}, #{end_date}, #{category}, #{participants}, #{funding_cost}, #{description}, #{mentor_chk}, #{deadline})
	</insert>
</mapper>